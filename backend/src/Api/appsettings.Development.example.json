{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Api.Services": "Debug"
    }
  },
  "OpenAI": {
    "//": "Prefer setting OPENAI_API_KEY in your environment. This file is a template — copy to appsettings.Development.json (gitignored) if you want the key to live on disk locally.",
    "ApiKey": ""
  }
}
